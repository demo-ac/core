# Orb 'circleci/hello-build@0.0.5' resolved to 'circleci/hello-build@0.0.5'
version: 2.1

orbs:
  masonite: masonite/trigger@dev:latest

workflows:
  version: 2
  run_worflow:
    jobs:
      - masonite/trigger:
          name: "Masonite Validation"
          repo: masoniteframework/validation
          branch: circle-ci
          environment: "MASONITE_DEPENDENT_BRANCH=$CIRCLE_BRANCH"
      - masonite/deploy:
          username: josephmancuso
